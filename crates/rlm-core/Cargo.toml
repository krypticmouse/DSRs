[package]
name = "rlm-core"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
publish = false
description = "Core traits for RLM type description and introspection"

[dependencies]
# No required dependencies - traits are pure Rust

[features]
default = []

[dev-dependencies]
